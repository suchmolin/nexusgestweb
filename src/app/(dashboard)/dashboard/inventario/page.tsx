export default function InventarioPage() {
  return (
    <div className="p-6 md:p-8">
      <h1 className="text-2xl font-bold text-[var(--foreground)]">Inventario</h1>
      <p className="text-[var(--muted)] mt-1">Productos, ingresos, egresos y consultas.</p>
      <div className="mt-6 p-4 rounded-lg bg-[var(--card)] border border-[var(--border)]">
        Módulo en construcción. Alta de productos, ingreso/egreso de stock, historial por producto.
      </div>
    </div>
  );
}
