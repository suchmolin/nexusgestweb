export default function LogsPage() {
  return (
    <div className="p-6 md:p-8">
      <h1 className="text-2xl font-bold text-[var(--foreground)]">Logs</h1>
      <p className="text-[var(--muted)] mt-1">Registro de acciones del sistema.</p>
      <div className="mt-6 p-4 rounded-lg bg-[var(--card)] border border-[var(--border)]">
        Módulo en construcción. Filtros por empresa, usuario, acción, rango de fechas.
      </div>
    </div>
  );
}
